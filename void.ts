const annonUser = (num:number): void => {
    for(let i = 0; i < num; i++) {
        alert('HIIIII!');
    }
}

function printTwice(msg:string): void  {     //does not return anything
    console.log(msg)
    console.log(msg)
}